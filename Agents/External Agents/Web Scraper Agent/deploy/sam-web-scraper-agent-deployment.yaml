apiVersion: apps/v1
kind: Deployment
metadata:
  name: sam-web-scraper-agent
  namespace: sam-solace-lab-agents
  labels:
    app: sam-external-agents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sam-external-agents
  template:
    metadata:
      labels:
        app: sam-external-agents
    spec:
      containers:
      - name: sam-web-scraper-agent
        image: registry.solace.lab/sam-web-scraper-agent:1.0.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        workingDir: /app
        command: ["solace-agent-mesh", "run"]
        args:
        - "configs/agents/web_scraper_agent.yaml"
        envFrom:
        # Shared values (LLM, Solace, S3); agent-specific secret overrides
        - secretRef:
            name: sam-shared-secret
        - secretRef:
            name: sam-web-scraper-agent-secret
        volumeMounts:
        - name: config-volume
          mountPath: /app/configs
        - name: dshm
          mountPath: /dev/shm
        resources:
          requests:
            cpu: "1000m"
            memory: "1Gi"
          limits:
            cpu: "2000m"
            memory: "4Gi"
      volumes:
      - name: config-volume
        configMap:
          name: sam-web-scraper-agent-config
          items:
          - key: web_scraper_agent.yaml
            path: agents/web_scraper_agent.yaml
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 2Gi
      imagePullSecrets:
      - name: localhost-registry
